{
  "name": "chrome-extension-boilerplate",
  "description": "Chrome插件模板",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite --config vite.config.ts build && vite --config vite.config2.ts build"
  },
  "dependencies": {
    "preact": "^10.5.15"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.1.5",
    "@types/chrome": "^0.0.184",
    "@types/node": "^17.0.31",
    "sass": "^1.51.0",
    "typescript": "^4.5.4",
    "vite": "^2.9.7"
  }
}
